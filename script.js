// Select the video element

// const video = document.querySelector("video");

// // Pause the video when it ends

// video.addEventListener("ended", function () {
//   this.pause();
// });

// Add a class to the current version of HTML in the list

// const versions = document.querySelectorAll("li");

// versions.forEach(function (version) {
//   if (version.textContent.includes("HTML5")) {
//     version.classList.add("current");
//   }
// });
